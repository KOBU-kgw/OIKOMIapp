# OIKOMI v1.3 Update Plan — Refinement Update

**計画策定日**: 2026-04-23  
**リリース目標**: 5月下旬〜6月初旬（v1.5 7月目標への橋渡し）  
**コンセプト**: 新機能追加は最小限。既存コアの「縁」を磨き、v1.5 Premium投入の土台を整える。  
**位置づけ**: v1.2（i18n）でユーザー層拡大 → v1.3（コア品質確定）→ v1.5（Premium投入）

---

## 1. スコープ（確定）

本バージョンで扱う4領域：

1. **TGL計算ロジックの改訂** — εクリフ平滑化、overdue分離、閾値再調整、通知逆算の書き直し
2. **ソート順の安定化** — TGLほぼ同値タスクのチラつき対策
3. **削除の寛容化** — Undoスナックバーによる取り消し可能化
4. **タスク操作パターン刷新** — タップで詳細（編集）画面、スワイプは「完了」のみに限定

### 意図的に含めないもの（v1.4以降）

- 通知メッセージのバリエーション化
- 空状態画面の刷新
- 統計・ヒートマップ機能
- Widget対応
- カスタムTGL閾値（v1.5 Premium）
- 設定画面の拡張

---

## 2. 柱1：TGL計算ロジック改訂

### 2.1 現行モデルの問題

- `slack = max(D - T, ε)` が D = T 近傍で急騰・天井張り付きを起こす
- D < T（期限超過）領域で TGL がフラットになり、深刻度を表現できない
- 通知スケジュールの逆算が不連続の影響を受け、遷移タイミングが粗くなる

### 2.2 新モデル（softplus による平滑化）

```dart
const double epsilon = 0.01;
const double kSmoothness = 5.0; // 暫定値、Calibration Phaseで確定

double softplus(double x, double k) {
  final kx = k * x;
  if (kx > 30) return x;                    // オーバーフロー回避（上側）
  if (kx < -30) return math.exp(kx) / k;    // オーバーフロー回避（下側）
  return math.log(1 + math.exp(kx)) / k;
}

double calculateTGL(Task task) {
  final now = DateTime.now();
  final D = task.deadline.difference(now).inMinutes / 60.0;
  final T = task.requiredHours;
  final M = task.avoidance.toDouble();

  final slack = softplus(D - T, kSmoothness) + epsilon;
  return (T * M) / slack;
}
```

k は「どれだけ現行の挙動を残すか」の調整ノブ。大きいほど現行寄り、小さいほど早めに立ち上がる。

### 2.3 overdue状態の独立扱い

期限を過ぎたタスク（D < 0）はTGLとは別軸で判定。

```dart
enum TGLState {
  peaceful,
  someday,
  reality,
  noEscape,
  war,
  overdue, // NEW: 期限超過
}

TGLState tglToState(Task task) {
  final now = DateTime.now();
  final D = task.deadline.difference(now).inMinutes / 60.0;

  if (D < 0) return TGLState.overdue;

  final tgl = calculateTGL(task);
  if (tgl < TGLThresholds.peaceful)  return TGLState.peaceful;
  if (tgl < TGLThresholds.someday)   return TGLState.someday;
  if (tgl < TGLThresholds.reality)   return TGLState.reality;
  if (tgl < TGLThresholds.noEscape)  return TGLState.noEscape;
  return TGLState.war;
}
```

**UI上の扱い**:
- 一覧では戦争タスクの**さらに上**に独立バンドとして表示
- 色はdeep crimson系（`#7f1d1d`）で戦争（`#ef4444`）と区別
- ラベル文言は i18n 検討時に確定（候補：「超過」「タイムアップ」「OVER」）

### 2.4 閾値再調整（Calibration Phase）

新モデル適用により、閾値 `0.4 / 1.5 / 6 / 20` の意味合いが微妙にズレる。特に D ≈ T 近辺で状態が早めに上がる傾向が予想される。

**検証方法**:
- 代表的タスクセット 50〜100件のシミュレーション
  - (T, M, D) の組み合わせを網羅
  - 現行モデルと新モデルで状態遷移タイミングを比較
  - 「戦争」に入る早さが現行比で許容範囲か確認
- k を 3, 5, 7, 10 でテストし、最適値を決定
- 必要なら閾値を再調整（例: `noEscape` 20 → 25 など）

このフェーズはWeek 1に実施。結果は別ドキュメント `v1.3_TGL_CALIBRATION.md` に記録。

### 2.5 通知スケジューリングの数値解法化

現行モデルの解析解（`D = T + (T*M)/threshold`）は新モデルでは使えないため、二分探索で各閾値への到達時刻を逆算する。

```dart
DateTime? _calculateTransitionTime(Task task, TGLState state) {
  final threshold = _thresholdFor(state);
  if (threshold == null) return null;

  final T = task.requiredHours;
  final M = task.avoidance.toDouble();
  final targetSlack = (T * M) / threshold;

  // slack は D について単調増加なので二分探索で安全に解ける
  double lo = 0.0;
  double hi = 24.0 * 365; // 上限1年

  for (int i = 0; i < 40; i++) { // 40回で十分な精度
    final mid = (lo + hi) / 2;
    final slack = softplus(mid - T, kSmoothness) + epsilon;
    if (slack < targetSlack) {
      lo = mid;
    } else {
      hi = mid;
    }
  }

  final requiredD = (lo + hi) / 2;
  return task.deadline.subtract(
    Duration(milliseconds: (requiredD * 3600 * 1000).toInt())
  );
}
```

### 2.6 T > D（そもそも間に合わない）ケース

新規追加時、登録時点で D < T のタスクは「hopeless タスク」として通知メッセージを分岐。

```dart
String notificationMessage(TGLState state, {bool isHopeless = false}) {
  if (isHopeless) return '時間が足りない。優先順位の再考を。';
  switch (state) {
    case TGLState.someday:  return '未来の自分が泣いてる';
    case TGLState.reality:  return 'そろそろ現実を見ようか';
    case TGLState.noEscape: return '逃げ場なくなりました';
    case TGLState.war:      return '戦争です。以上。';
    case TGLState.overdue:  return '期限超過。今すぐ確認。';
    default:                return '';
  }
}
```

### 2.7 SPEC.md への反映ポイント

- `calculateTGL` の実装置換
- `TGLState` に `overdue` 追加、`tglToState` を Task受け取り型に変更
- `_calculateTransitionTime` を二分探索版に置換
- `_thresholdFor` の戻り値をnullableに
- `notificationMessage` に `isHopeless` パラメータ追加
- 一覧画面の `overdue` バンド表示仕様を追記

---

## 3. 柱2：ソート順の安定化

### 3.1 問題

TGLがほぼ同値のタスクが並ぶと、計算ごとの微細な差（時刻進行による）でカード位置が毎秒入れ替わり、見づらい。

### 3.2 解決策：多段タイブレーカー

```dart
tasks.sort((a, b) {
  final now = DateTime.now();

  // 1. overdue を最上位に
  final aOverdue = a.deadline.isBefore(now);
  final bOverdue = b.deadline.isBefore(now);
  if (aOverdue != bOverdue) return aOverdue ? -1 : 1;

  // 2. TGL降順（0.05未満の差は同値とみなす）
  final tglA = calculateTGL(a);
  final tglB = calculateTGL(b);
  if ((tglA - tglB).abs() > 0.05) {
    return tglB.compareTo(tglA);
  }

  // 3. 締切昇順
  final deadlineDiff = a.deadline.compareTo(b.deadline);
  if (deadlineDiff != 0) return deadlineDiff;

  // 4. createdAt 昇順（最終タイブレーカー）
  return a.createdAt.compareTo(b.createdAt);
});
```

これで数値が揺れても順序が安定する。閾値 `0.05` は検証後に調整可能（Calibration Phaseで一緒に決める）。

---

## 4. 柱3：削除の寛容化（Undoスナックバー方式）

### 4.1 データモデル変更

```dart
class Task {
  // ... 既存フィールド
  DateTime? deletedAt; // NEW: 論理削除タイムスタンプ
}
```

一覧クエリの条件：`deletedAt == null && !isCompleted`

### 4.2 削除フロー

1. ユーザーが削除ボタンタップ
2. `deletedAt = DateTime.now()` を設定 → 一覧から即座に消える
3. 画面下部に SnackBar 表示「課題を削除しました [取り消し]」（5秒）
4a. 「取り消し」タップ → `deletedAt = null` に戻す → 一覧に復活
4b. 5秒経過 → バックグラウンドで物理削除、通知スケジュールもキャンセル

### 4.3 完了操作にも同パターン適用

「完了」操作もUndoスナックバーで取り消し可能化（`isCompleted` フラグ + 5秒猶予）。モデル的に `completedAt` を追加して削除と同じ仕組みに統一。

### 4.4 実装メモ

- Flutter標準の `ScaffoldMessenger.of(context).showSnackBar()` を使用
- 論理削除中のタスクは通知スケジュールをpause（物理削除時に完全キャンセル）
- アプリ再起動時、`deletedAt` が5秒以上前のものは起動時バッチで物理削除
- i18n 対象：SnackBarメッセージ、「取り消し」アクション文言

### 4.5 SPEC.md への反映ポイント

- `Task` モデルに `deletedAt`, `completedAt` 追加
- 削除・完了操作の挙動を再定義
- 一覧の表示条件更新

---

## 5. 柱4：タスク操作パターン刷新

### 5.1 新しい操作体系

| 操作 | 挙動 |
|---|---|
| タスクカードをタップ | 詳細（＝編集）画面へ遷移 |
| 左スワイプ | 「✓ 完了」ボタンのみ表示（即時操作用） |
| 詳細画面ヘッダーボタン | 完了 / 削除 |

**「編集」「削除」ボタンはスワイプから撤廃**。タップ遷移に一本化。

### 5.2 詳細画面＝編集フォーム

タップ直後からフォームが編集可能な状態で開く（案α方式採用）。既存の `TaskFormScreen` を拡張して兼用。

### 5.3 画面構成

```
┌─────────────────────────────┐
│ [＜戻る]   [✓完了] [🗑削除] │ ← ナビバー
├─────────────────────────────┤
│ ┌─────────────────┐         │
│ │  戦争           │         │ ← 状態ラベル（ヘッダー、大）
│ └─────────────────┘         │
│                             │
│ 課題名 [物理レポート]       │ ← 編集可能
│                             │
│ タイプ [レポート ▾]         │
│                             │
│ 締切  [12/20] [23:59]       │
│                             │
│ 所要時間 [3h] / やる気 [7]  │ ← ドラムピッカー
│                             │
│ [保存する]                  │
└─────────────────────────────┘
```

**状態ラベルの扱い**:
- overdue状態の場合は赤帯＋カウントダウン「3時間超過」等を表示
- 戦争・逃げ場なしは背景色で視覚的に強調
- TGL数値は引き続き**非表示**（v1.5でもこの方針は変わらず）

### 5.4 スワイプ「完了」の仕様

- 左スワイプで右端に「✓ 完了」ボタン1つが現れる
- タップで `completedAt = DateTime.now()` に更新、一覧から即消える
- 完了操作にもUndoスナックバー適用（5秒猶予、柱3と統合）

### 5.5 画面遷移ルール

| 遷移元 | 遷移先 | トリガー |
|---|---|---|
| 一覧画面 | タスク追加画面 | ナビバー右上「+」 |
| 一覧画面 | タスク詳細（編集）画面 | カードタップ |
| タスク詳細画面 | 一覧画面 | ナビバー左「戻る」 / 保存完了 / 削除完了 / 完了操作 |

### 5.6 SPEC.md への反映ポイント

- 「タスク一覧画面」の操作セクションを全面書き換え
- 「編集画面」を「タスク詳細画面」にリネーム、状態ラベルのヘッダー表示を追加
- タップ遷移ルールを明記
- `TaskFormScreen` → `TaskDetailScreen`（または兼用コンポーネント名の再検討）

---

## 6. 実装順序とマイルストーン

**スコープ**: 3週間 + 予備1週間 = 計4週間

### Week 1: TGL計算の書き換え
- `calculateTGL` / `softplus` 実装
- `TGLState.overdue` 追加と判定分岐
- 通知逆算の二分探索実装
- 単体テスト全書き直し（代表シナリオ9点 + overdueケース）
- **Calibration Phase**（k値と閾値の決定、シミュレーション）
- `v1.3_TGL_CALIBRATION.md` 作成

### Week 2: 画面遷移パターン刷新
- `TaskDetailScreen`（詳細＝編集兼用）実装
- 状態ラベルヘッダー実装
- 一覧カードのタップハンドラ接続
- スワイプメニューを「完了」のみに削減
- Reactモックでのレビュー（必要なら）

### Week 3: 削除寛容化とソート安定化
- `Task.deletedAt` / `completedAt` 追加、論理削除ロジック
- UndoスナックバーUI（削除・完了両方）
- 起動時バッチでの物理削除
- ソート安定化（タイブレーカー実装）
- i18n 反映（SnackBarメッセージ、overdue ラベル等）

### Week 4: 予備（QA・バグ修正・リリース準備）
- iOS / Android 両プラットフォームでの動作確認
- TestFlight / Play Closed Testing へのデプロイ
- ストア説明文の更新
- スクリーンショット差し替え（詳細画面のUI変更のため要更新）
- リリースノート執筆

---

## 7. リスクとオープン項目

### リスク1：k値の決定が難しい
- **対策**: Week 1 の Calibration Phase で複数値テスト、現行モデルとの乖離を可視化
- **保守案**: k を大きめ（8〜10）に設定して現行挙動にほぼ寄せる

### リスク2：通知逆算の数値解法で精度不足
- **対策**: 二分探索40回で0.0001h未満の精度（約0.4秒）。通知タイミングとして十分
- **テスト**: 境界ケース（threshold ≈ T*M/ε の極端値）での挙動確認

### リスク3：スワイプUI変更によるユーザー混乱
- **対策**: v1.3リリースノートに明記、初回起動時のヒント表示を検討（別issue化）
- **補足**: iOS先行リリースユーザーにはメール or 通知で事前告知も検討

### リスク4：スコープ肥大による遅延
- **対策**: Week 4 を予備として明示的に確保
- **判断**: Week 3終了時点で未完なら、ソート安定化のタイブレーカー閾値チューニング等の「磨き」項目をv1.4送り

### オープン項目
- [ ] ストアページのスクリーンショット更新（詳細画面のUIが変わるため）
- [ ] `overdue` 状態のラベル文言確定（日本語・英語）
- [ ] 既存ユーザー向けリリースノート文言
- [ ] Androidでのスワイプ挙動確認（Play Closed Testing進行中のため検証機会あり）
- [ ] Reactインタラクティブモック作成要否（詳細画面レイアウトのみ）

---

## 8. v1.5への繋ぎ

v1.3で以下を確定させることで、v1.5 Premium実装が楽になる：

- **TGL計算モジュールが切り出し済み** → カスタム閾値UIから値を差し込むだけで済む
- **詳細画面＝編集フォーム兼用パターンの確立** → Premium設定UIの実装パターンとして流用可能
- **UndoスナックバーパターンUX確立** → プロファイル切り替え時の「取り消し」UXにも応用
- **overdue状態の独立扱い** → Premiumでカスタム閾値を弄っても「期限超過」の扱いがブレない

---

**Document Version**: 1.0  
**Status**: ドラフト完成（実装着手前）  
**Next Action**: Week 1 キックオフ / SPEC.md 差分作成 / Calibration Phase設計
