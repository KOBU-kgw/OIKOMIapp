# OIKOMI v1.5 — Claude Code 実装プロンプト（改訂版 r2）

## メタ情報

| 項目 | 内容 |
|---|---|
| アプリ名 | OIKOMI |
| バージョン | v1.4 → v1.5 |
| リリース目標 | 2026年7月上旬〜中旬（前期期末試験ピーク前にストア公開） |
| 位置づけ | 有料機能の初投入（**買い切り1 SKU**）＋ TGL計算エンジン強化 |
| プラットフォーム | iOS / Android 同時（Apple IAP ＋ Google Play Billing） |
| パッケージ名 | `com.kobu.oikomi` |

### r2での主な変更（旧版からの差分）

- 課金を **サブスク3プラン → 買い切り1 SKU（`oikomi.unlock.thresholds`）** に簡素化
- 呼称を「Premium」から **「カスタム閾値パック」** に変更（将来のサブスクと名前空間を分離するため。UI・ストア説明で「Premium」という語は使わない）
- サブスク管理（有効期限・トライアル・グレースピリオド・失効フォールバック）を**全削除**
- サブスク（統計・クラウド同期）は v1.7〜2.0 の構想として**社内ロードマップに記録のみ。ユーザーには予告しない**

### このバージョンの2本柱

1. **[FEAT-01] マルチタスクslack補正** — 先行タスクの所要時間を余裕時間から差し引く、より現実に近いTGL計算（**全ユーザー無料**）
2. **[FEAT-02] カスタム閾値パック** — カスタムTGL閾値（買い切りIAPで解放）

補助:

3. **[FEAT-03] 転換導線** — 初回起動7日目前後の「閾値認知」ナッジ＋カスタム閾値プレビュー

---

## 前提知識（必ず読むこと）

このアプリは **TGL（Task Gotta Level）** という独自指標で課題の「精神的ヤバさ」を管理するタスク管理アプリ。

基本式: `TGL = (T × M) / slack`

- `T` = 所要時間（時間）
- `M` = やりたくなさ（1〜10）
- `slack` = 実効的な余裕時間

現行実装（v1.3〜v1.4で導入済み）の前提:

- **実効稼働時間補正（v1.4）**: カレンダー時間に `kActiveRatio = 0.75`（18h/日稼働）を掛けて実効時間化
- **softplus平滑化（v1.3）**: `max(D - T, ε)` の代わりにsoftplusベースの滑らか関数でεクリフを解消済み
- **overdue状態（v1.3）**: 期限超過は独立状態として扱い、「戦争」に押し込めない
- **通知逆算（v1.3）**: 解析式ではなく**二分探索ソルバー**で状態遷移時刻を求める
- **ソート安定化（v1.3）**: 多段タイブレーカー（overdue → TGL差分0.05閾値 → 締切 → createdAt）

> ⚠️ 上記の既存実装コードはリポジトリ内の `tgl_calculator.dart` / `notification_service.dart` を正とする。本書のコードは**実装方針を示す擬似コード**であり、既存のsoftplus関数・二分探索ソルバーをそのまま流用して拡張すること。

### 絶対不変の制約

- **TGLの生数値はユーザーに絶対表示しない**（状態ラベルのみ）
- **コア式 `(T × M) / slack` の構造は変更しない**
- 通知オフでもアプリは完全に使用可能であること
- iOS通知上限64件を超えない
- サーバーレス（ローカル完結）を維持。外部アナリティクスは導入しない

---

## [FEAT-01] マルチタスクslack補正（無料コア機能）

### 問題

現在のslackは「そのタスク単体の締切までの実効時間 − そのタスクの所要時間」で計算している。
しかし実際には、**そのタスクより先に締め切られる他のタスク**にも時間を取られるため、体感より余裕が多く見積もられ、TGLが過小評価される。

### 設計

#### 先行タスクの定義

```
P(i) = { j | j.isCompleted == false
           かつ j.id != i.id
           かつ (j.deadline < i.deadline
                または (j.deadline == i.deadline かつ j.createdAt < i.createdAt)) }
```

- 締切が早いタスクを「先行」とみなす
- 同時刻締切は createdAt の早い方を先行とする（既存タイブレーカーと整合）
- 完了済み・削除済み（論理削除中含む）は除外

#### slack計算の変更

```dart
// 擬似コード。既存のsoftplus平滑化関数 smoothSlack() を流用すること
double calculateTGL(Task task, List<Task> incompleteTasks) {
  final now = DateTime.now();
  final calendarHours = task.deadline.difference(now).inMinutes / 60.0;
  final effectiveHours = calendarHours * kActiveRatio;

  // 先行タスクの所要時間合計（NEW）
  final precedingLoad = incompleteTasks
      .where((j) => _isPreceding(j, task))
      .fold(0.0, (sum, j) => sum + j.requiredHours);

  final T = task.requiredHours;
  final M = task.avoidance.toDouble();

  final rawSlack = effectiveHours - T - precedingLoad; // ← precedingLoad を追加
  final slack = smoothSlack(rawSlack); // v1.3のsoftplus平滑化をそのまま適用

  return (T * M) / slack;
}
```

> **シグネチャ変更あり**: `calculateTGL(Task task)` → `calculateTGL(Task task, List<Task> incompleteTasks)`。
> 呼び出し側（一覧画面・通知サービス・起動時再計算）をすべて更新すること。
> 一覧表示時は締切昇順ソート＋累積和（prefix sum）で O(N log N) で全タスク分を計算できる。タスクごとにループを回さない。

#### overdueとの関係

- overdue判定（締切超過）のロジックは**変更しない**。precedingLoadはoverdue判定に影響させない
- 先行タスクがoverdueの場合もP(i)に含める（残っている以上、時間は食われるため）

### 通知への波及（最重要の設計判断）

precedingLoadは他タスクの追加・編集・完了・削除で変わるため、**任意のタスク操作で全タスクの通知を再スケジュールする**必要がある。

#### 再スケジュール戦略

```
トリガー: タスクの追加 / 編集（deadline・T・M・完了状態の変更） / 削除 / Undo復元 / アプリ起動
処理:
  1. 全予約済み通知をキャンセル
  2. 未完了タスクを締切昇順でソートし precedingLoad を累積計算
  3. 各タスク×各状態遷移（最大4件）の遷移時刻を二分探索ソルバーで算出
     - ソルバー内のslack計算にも precedingLoad を反映（その時点のタスク集合で固定値として扱う）
  4. 通知予算チェック（下記）を通過したものだけ予約
```

#### 通知予算（64件上限対策）

```
予算上限: 60件（バッファ4件）
超過時の優先順位: 遷移トリガー時刻の昇順（近い未来を優先）で60件まで採用し、残りは破棄
補充: 破棄分は次回の全再スケジュール（タスク操作 or アプリ起動）で自然に再評価される
```

#### 実装上の注意

- 連続したタスク操作（Undoスナックバー猶予中など）で再スケジュールが連打されないよう、**300〜500msのdebounce**を入れる
- Undoスナックバーの論理削除中タスクは**論理削除即時にP(i)から除外し、Undo時に再スケジュール**とする（ユーザーの意図は削除なので）

### テストシナリオ（FEAT-01）

| # | シナリオ | 期待 |
|---|---|---|
| 1 | タスク1件のみ | precedingLoad=0、v1.4と同一のTGL |
| 2 | 締切が近いタスクAを追加すると、締切が遠いタスクBのTGLが上がる | B.slackがA.Tぶん減る |
| 3 | タスクAを完了するとBのTGLが下がり、Bの通知が再予約される | 通知トリガー時刻が後ろ倒しになる |
| 4 | 同時刻締切2件 | createdAt順で片方向のみ先行扱い（相互参照しない） |
| 5 | 先行タスクがoverdue | P(i)に含まれ、後続のslackを圧迫する |
| 6 | タスク20件×遷移4件=80件 | 予約は60件以下、近い順に採用されている |
| 7 | 全タスクのprecedingLoad計算 | O(N²)ループになっていない（累積和実装） |

---

## [FEAT-02] カスタム閾値パック（買い切りIAP）

### 課金設計の方針（重要・経緯の記録）

- **買い切り1 SKUのみ。サブスクはv1.5では導入しない**
- 機能の性質に課金形態を一致させる:
  - カスタム閾値 = 一回設定したら終わり・サーバーコストゼロ → **買い切り**
  - 統計・クラウド同期（v1.7〜2.0構想）= 継続的価値・サーバー実費 → **将来のサブスク**
- **名前空間の分離**: 買い切りを「Premium」と呼ぶと、将来のサブスク機能が「Premiumなのに含まれない」となり信頼を損なう。買い切りは**「カスタム閾値パック」**という狭い名前で売り、対象機能をストア説明に明記する
- 将来サブスクを導入する際、サブスクは買い切りの**上位互換（カスタム閾値も含む全部入り）**とし、権利マトリクスを3状態に保つ:

| | 無料 | 閾値パック（v1.5〜） | サブスク（v1.7〜構想） |
|---|---|---|---|
| コア機能＋slack補正 | ✓ | ✓ | ✓ |
| カスタム閾値 | — | ✓ | ✓ |
| 統計・クラウド同期 | — | — | ✓ |

> サブスク構想はユーザーに**予告しない**（約束→未実装の信頼毀損を避ける）。本書とBUSINESS_DECISION.mdの改訂版にのみ記録する。

### IAP商品定義

| SKU ID | 商品名（ja） | 価格 | 種別 |
|---|---|---|---|
| `oikomi.unlock.thresholds` | カスタム閾値パック | $4.99 USD（一度きり） | 非消耗型（買い切り・トライアルなし） |

- **SKU IDは確定値**（作成後変更不可）。両ストアとも同一IDで作成する
- 価格は両ストアの自動換算Tierを申請前に確認（日本円の目安: ¥800前後。不自然なら近いTierに調整）
- Play側: 「アプリ内商品（非消耗型）」として登録。定期購入は作成しない
- ストア内の商品説明（両ストア共通の趣旨）:
  > 「まだ平和」から「戦争」まで、5段階のラインをあなた仕様に調整できるようになります。一度の購入で永続利用できます。対象はカスタム閾値機能です。

### 機能スコープ（v1.5）

| 含む | 含まない（将来） |
|---|---|
| カスタム閾値の編集（4値: peaceful / someday / reality / noEscape） | 閾値プロファイル3セット保存・切替（v1.6候補） |
| ラベル即時反映プレビュー | 統計・ヒートマップ（v1.7〜サブスク構想） |
| 1タップでデフォルト値にリセット | クラウド同期（v1.7〜2.0サブスク構想） |
| 購入・復元導線（両ストア） | 通知メッセージカスタマイズ |

> **プレビューに関する仕様注記**: ソートはTGL生値で行われ、閾値はソートに影響しない。プレビューで変わるのは**状態ラベルと通知タイミング**。UIコピーもそれに合わせること（例:「あなたの『戦争』が始まるラインを決めよう」）。

### 閾値の制約

| 閾値 | デフォルト | 可動範囲 |
|---|---|---|
| peaceful | 0.4 | 0.1〜2.0 |
| someday | 1.5 | 0.5〜5.0 |
| reality | 6.0 | 2.0〜15.0 |
| noEscape | 20.0 | 5.0〜50.0 |

- **狭義単調増加を強制**: peaceful < someday < reality < noEscape。スライダーUIで隣接値を越えられないようクランプする
- 閾値変更を**保存（コミット）した時点で**通知を全再スケジュール（スライダー操作ごとには再スケジュールしない）
- overdue状態はカスタム閾値の影響を受けない（独立状態のまま）

### 技術実装

#### パッケージ

```yaml
dependencies:
  in_app_purchase: ^3.x  # iOS StoreKit / Google Play Billing 両対応。最新安定版を確認
```

#### データモデル（isar新コレクション）

```dart
@collection
class UserPreference {
  Id id = 0; // シングルトン（常にid=0）

  // 購入状態（買い切りなので有効期限・猶予関連フィールドは持たない）
  bool isThresholdsUnlocked = false;
  String? lastPurchaseId; // 重複防止用トランザクションID

  // カスタム閾値（値は常に保持。適用は isThresholdsUnlocked && useCustomThresholds のときのみ）
  double customPeaceful = 0.4;
  double customSomeday = 1.5;
  double customReality = 6.0;
  double customNoEscape = 20.0;
  bool useCustomThresholds = false;

  // 転換導線用（FEAT-03）
  DateTime? firstLaunchAt;
  int nudgeShownCount = 0;
}
```

変更後 `flutter pub run build_runner build` を必ず実行。

#### 購入状態の解決ルール（シンプル）

```
1. 購入完了イベント受領 → isThresholdsUnlocked = true を永続化
2. 「購入を復元」実行 → ストアの購入履歴に oikomi.unlock.thresholds があれば true
3. 失効処理は存在しない（買い切りのため）。期限チェック・グレースピリオド・
   定期検証は一切実装しない
4. 機種変更・再インストール時は復元ボタンで回復（オンボーディングで案内不要、
   購入画面に常設）
```

#### 閾値の参照経路

`tglToState()` と通知ソルバーの閾値参照を、ハードコードの `TGLThresholds` から
`ThresholdProvider.current()`（解放済み＋有効化ならカスタム値、それ以外はデフォルト）経由に差し替える。
**TGL計算式そのものには一切手を入れない**（閾値はラベル変換と通知逆算にのみ作用する）。

#### 画面構成

| 画面 | 内容 |
|---|---|
| 設定 > カスタム閾値 | 未購入なら購入画面へ、購入済みなら閾値エディタへ |
| 購入画面 | 単一商品の提示（価格・「一度の購入で永続利用」・対象機能の明記）、購入ボタン、**「購入を復元」ボタン**、利用規約・プライバシーポリシーへのリンク |
| 閾値エディタ | 4スライダー＋ライブラベルプレビュー＋リセットボタン。保存時に通知再スケジュール |

#### i18n

課金関連の全テキストをARB（ja/en）に追加。既存キー `settingsRestorePurchase`（v1.2で予約済み）を実体化し、v1.1の「近日公開」ダイアログを撤去する。**「Premium」という文言はUI・ARBキー名の双方で使用しない**（`unlock` / `thresholdPack` 系で命名）。

### ストア審査要件チェックリスト

#### 共通

- [ ] 購入画面に「価格・買い切りであること・対象機能」を明示（虚偽・誇張なし）
- [ ] 「購入を復元」が機能する（別端末での復元テスト済み）
- [ ] 課金しなくてもコア機能（タスク管理・デフォルト閾値・通知・slack補正）が完全動作する
- [ ] プライバシーポリシー・利用規約リンクが購入画面とストアメタデータの両方にある

#### iOS（App Store Connect）

- [ ] 非消耗型IAP `oikomi.unlock.thresholds` を作成、日英の商品説明登録
- [ ] App内課金の審査用スクリーンショット添付
- [ ] Sandboxテスターで 購入 → 再インストール → 復元 のフロー確認

#### Android（Google Play Console）

- [ ] **前提: Play本番公開が可能な状態であること**（個人デベロッパーアカウントのクローズドテスト要件——テスター数・期間——を満たしているかPlay Consoleで確認。未達ならiOS先行に切り替える判断を Week 0 で行う）
- [ ] アプリ内商品（非消耗型）`oikomi.unlock.thresholds` の登録
- [ ] ライセンステスター登録、テスト購入・復元フロー確認
- [ ] 課金ありに伴うデータセーフティ・コンテンツレーティングの再申告確認

---

## [FEAT-03] 転換導線（ナッジ＋プレビュー）

### 仕様

- **トリガー**: `firstLaunchAt` から7日経過後の最初の起動時
- **表示**: 一覧画面上部に1枚のディスミス可能なカード
  - コピー案: 「『戦争』が始まるタイミング、人によって違うらしい。」→ タップで閾値プレビューへ
- **プレビュー画面**: 閾値エディタと同一UIを**読み取り専用デモ**として開放。サンプルタスク（または実タスク）でラベルが変わる様子を体験できる。保存しようとした時点で購入画面へ
- **頻度制限**: 表示は最大2回（初回ディスミス後、14日後に1回だけ再表示）。`nudgeShownCount` で管理
- **絶対条件**: コア機能を一切ブロックしない。フルスクリーン化・強制表示はしない（審査リスク＋体験毀損）

---

## 計測と判断基準（サーバーレス前提）

外部アナリティクスは導入しない。**両ストアコンソールで取れる指標のみ**で判断する。買い切り1 SKUなので指標もシンプルになる。

| 指標 | 取得元 | 60日時点の判断ライン（目安） |
|---|---|---|
| 購入数 / MAU | App Store Connect / Play Console | 3%未満: 価値が伝わっていない → ナッジ文言・プレビュー改善（v1.5.x）。8%以上: 閾値カスタムの価値仮説が成立 → v1.6でプロファイル等を積み増し |
| 返金率 | 同上 | 高ければ「期待と実物のギャップ」のシグナル → 商品説明・プレビューの見直し |

> アプリ内ファネル（ナッジ表示→タップ→購入画面到達）はローカル完結のため計測不可。これは**プライバシー姿勢（全ローカル処理）を維持するための意図的なトレードオフ**として受け入れる。
>
> この検証結果は v1.7〜のサブスク（統計・同期）の価格設定・投入判断のインプットになる。

---

## スコープ外（将来バージョンに明示的に延期）

| 機能 | 時期目安 | 備考 |
|---|---|---|
| 閾値プロファイル3セット保存 | v1.6 | 閾値パック購入者への無償追加（買い切りの範囲内とする） |
| 統計・ヒートマップ | v1.7〜 | **サブスク構想の柱1**。ユーザーには予告しない |
| クラウド同期 / マルチデバイス | v1.7〜2.0 | **サブスク構想の柱2**。サーバーレス原則の破棄を伴うため、認証・運用・プライバシーポリシー全面改定とセットで計画する |
| サブスクSKUの設計 | v1.7計画時 | 買い切りの上位互換（全部入り）とする。既購入者の扱い（割引等）はその時点で判断 |
| 通知メッセージカスタマイズ | 未定 | ニーズ未確認 |
| kActiveRatioのユーザー設定化 | 未定 | 閾値カスタムと役割が近い。閾値パック拡張候補 |

---

## スケジュール（目安）

| 週 | 期間 | 内容 |
|---|---|---|
| Week 0 | 6/15–6/21 | v1.4リリース完了。両ストアでIAP商品作成（`oikomi.unlock.thresholds`）。Sandbox/ライセンステスター準備。**Play本番公開可否の確認（NGならiOS先行に方針転換）** |
| Week 1 | 6/22–6/28 | FEAT-01実装（slack補正＋通知全再スケジュール＋予算管理）＋ユニットテスト全PASS |
| Week 2 | 6/29–7/5 | FEAT-02実装（UserPreference / PurchaseService / 購入画面 / 閾値エディタ）＋i18n |
| Week 3 | 7/6–7/12 | FEAT-03実装。Sandbox・ライセンステストで購入/復元フロー確認。スクリーンショット更新。両ストア申請 |
| Week 4 | 7/13–7/19 | 審査対応バッファ。リリース。X（@kobu_jp）で試験シーズン投稿と連動した告知 |

> サブスク機構の削除により、Week 2-3の課金実装は旧計画（3プラン＋失効処理）より大幅に軽い。余剰が出た場合はFEAT-01のエッジケーステスト拡充に充てる。

---

## 実装時の注意事項（まとめ）

1. TGL生数値は絶対に表示しない。プレビューでも数値ではなくラベルの変化のみ見せる
2. コア式 `(T × M) / slack` は不変。slackの中身（precedingLoad）と閾値の参照先だけが変わる
3. 既存のsoftplus平滑化・二分探索ソルバー・多段タイブレーカーを壊さない（既存テストを全て維持）
4. 閾値変更・購入/復元・タスク操作のすべてで通知再スケジュールが正しく走ること
5. 課金失敗・通信不能時もアプリがクラッシュせず、無料機能が常に使えること
6. 「Premium」という呼称をコード・UI・ストア説明のどこにも使わない（`unlock` / `thresholdPack` 系で統一）
7. サブスク用のコード（期限管理等）を「将来のため」に先回り実装しない（YAGNI。v1.7計画時に設計する）
